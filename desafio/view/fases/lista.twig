<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>List</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    </head>
    <body>
        <div>
            <div class="header">
                <h2>Fases da Trilha - {{ trilha.titulo }}</h2>
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <a class="navbar-brand" href="#">Fases</a>
                    <a class="navbar-brand" href="{{  base_group_url ~ id_trilha ~ '/fase-incluir/' }}">Adicionar</a>
                </nav>
            </div>
            <div class="container">
                <div class="content">
                    <table class="table">
                        <thead>
                        <tr>
                            <th scope="col">Ordem</th>
                            <th scope="col">Titulo</th>
                            <th scope="col">Ativa</th>
                            <th scope="col"></th>
                            <th scope="col"></th>
                            <th scope="col"></th>
                            <th scope="col"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for registro in registros %}
                            <tr>
                                <td>{{ registro.ordem }}</td>
                                <td>{{ registro.titulo }}</td>
                                {% if (registro.ativa == 1) %}
                                    <td>ATIVO</td>
                                {% else %}
                                    <td>INATIVO</td>
                                {% endif %}
                                <td><a class='btn btn-outline-secondary' role='button' href='{{ registro.id }}/perguntas/'>Desafios</a></td>
                                <td><a class='btn btn-outline-secondary' role='button' href='editar/{{ registro.id }}'>Alterar</a></td>
                                <td><a class='btn btn-outline-secondary' role='button' href='excluir/{{ registro.id }}'>Excluir</a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </body>
</html>